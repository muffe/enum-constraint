{
    "name": "muffe/enum-constraint",
    "description": "A Symfony Validator constraint that validates if given strings are valid cases in a given PHP 8 Enum",
    "type": "library",
    "keywords": ["enum", "validator", "constraint"],
    "minimum-stability": "dev",
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Muffe\\EnumConstraint\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Marc Wustrack",
            "email": "mail@wustrack.dev"
        }
    ],
    "require": {
        "php": ">=8.1",
        "symfony/validator": "^6.1"
    }
}
